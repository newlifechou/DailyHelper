<Window
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	x:Class="DialyHelper.Login"
	x:Name="Window"
	Title="Login"
	Width="356" Height="291" WindowStartupLocation="CenterScreen" ResizeMode="NoResize" Icon="Images/calendar_green.png">
    <Grid x:Name="LayoutRoot" Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Daily Helper" HorizontalAlignment="Center" FontSize="40" FontFamily="/DialyHelper;component/Fonts/#Tekton Pro" Foreground="Blue"></TextBlock>
        <TextBlock Grid.Row="1" Text="Enter your unlock code below:" />
        <PasswordBox Name="txtpassword" Grid.Row="2" Margin="5" FontSize="24"/>
        <Button Name="btnunlock" Grid.Row="3" Content="Unlock"  Click="btnunlock_Click" IsDefault="True"/>
        <TextBlock Name="txtLogInfo" Grid.Row="4" Foreground="Red"/>
    </Grid>
</Window>